import React, { FC } from "react";
import CustomButton from "../ui-elements/buttons";

const Header = () => {
  return (
    <div>
      <CustomButton name="Route 1" />
    </div>
  );
};

export default Header;
